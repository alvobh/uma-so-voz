define(['application', 'libs/query_wrapper', 'libs/query_cache', 'services/atualizacao'], function(app, QueryWrapper, QueryCache, Atualizacao) {
/*
	Reflexao.create = function(feeds, callback) {
	    if(attrs.title && attrs.content && attrs.author && attrs.publishedDate && attrs.contentSnippet && attrs.media ) { 
	      var object = new Reflexao(attrs);
	      object.save(null, {
	        error: callback,
	        success: function(reflexao) {
	          Reflexao.cache.insert(reflexao);
	          callback(object);
	        }
	      }); 
	    }  
  	}
*/
});